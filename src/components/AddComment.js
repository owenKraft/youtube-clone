import React from 'react'

const AddComment = (props) => {

    return (
        <div className="add-comment">
            <i class="fas fa-user-circle"></i>
            <input placeholder="Sign in to add a comment" disabled></input>
        </div>
    )
}

export default AddComment